# AI-OS API — Phase 0 dependencies (pinned to majors, MVP-minimal)
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
sqlalchemy==2.*
psycopg[binary]==3.*
websockets==13.*

# Sandbox backend (Phase 2) — optional at runtime, imported lazily.
docker==7.*

# LLM provider (Phase 4) — optional at runtime, imported lazily.
anthropic==0.40.*

# Dev / tests
pytest==8.*
pytest-asyncio==0.24.*
httpx==0.27.*
